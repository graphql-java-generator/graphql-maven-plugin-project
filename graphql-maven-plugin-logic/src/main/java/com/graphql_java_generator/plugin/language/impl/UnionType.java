// Generated by delombok at Mon Dec 22 16:05:30 CET 2025
/**
 * 
 */
package com.graphql_java_generator.plugin.language.impl;

import java.util.ArrayList;
import java.util.List;
import com.graphql_java_generator.plugin.DocumentParser;
import com.graphql_java_generator.plugin.conf.CommonConfiguration;

/**
 * This class represents an Union, as defined in a GraphQL schema. In the generated classes, this needs to be an
 * interface, as an object type may be in several unions.
 * 
 * @author etienne-sf
 */
public class UnionType extends ObjectType {
	/**
	 * Contains the list of all types that are allowed in this union, in the GraphQL schema.
	 */
	List<ObjectType> memberTypes = new ArrayList<>();

	/**
	 * @param name
	 *            the name for this type
	 * @param configuration
	 *            The current plugin configuration, which is accessible through an interface that extends
	 *            {@link CommonConfiguration}
	 * @param documentParser
	 *            The {@link DocumentParser} that has parsed the schema, and so that contains the whole schema
	 *            definition
	 */
	public UnionType(String name, CommonConfiguration configuration, DocumentParser documentParser) {
		super(name, GraphQlType.UNION, configuration, documentParser);
	}

	@Override
	protected String getPrefix() {
		return configuration.getUnionPrefix();
	}

	@Override
	protected String getSuffix() {
		return configuration.getUnionSuffix();
	}

	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("UnionType {name=" + getName() + ", members=[");
		boolean appendSep = false;
		for (ObjectType m : memberTypes) {
			if (appendSep) sb.append(",");
			 else appendSep = true;
			sb.append(m.getName());
		}
		sb.append("]}");
		return sb.toString();
	}

	/**
	 * Contains the list of all types that are allowed in this union, in the GraphQL schema.
	 */
	
	
	public List<ObjectType> getMemberTypes() {
		return this.memberTypes;
	}

	/**
	 * Contains the list of all types that are allowed in this union, in the GraphQL schema.
	 */
	
	
	public void setMemberTypes(final List<ObjectType> memberTypes) {
		this.memberTypes = memberTypes;
	}

	@java.lang.Override
	
	
	public boolean equals(final java.lang.Object o) {
		if (o == this) return true;
		if (!(o instanceof UnionType)) return false;
		final UnionType other = (UnionType) o;
		if (!other.canEqual((java.lang.Object) this)) return false;
		if (!super.equals(o)) return false;
		final java.lang.Object this$memberTypes = this.getMemberTypes();
		final java.lang.Object other$memberTypes = other.getMemberTypes();
		if (this$memberTypes == null ? other$memberTypes != null : !this$memberTypes.equals(other$memberTypes)) return false;
		return true;
	}

	
	
	protected boolean canEqual(final java.lang.Object other) {
		return other instanceof UnionType;
	}

	@java.lang.Override
	
	
	public int hashCode() {
		final int PRIME = 59;
		int result = super.hashCode();
		final java.lang.Object $memberTypes = this.getMemberTypes();
		result = result * PRIME + ($memberTypes == null ? 43 : $memberTypes.hashCode());
		return result;
	}
}
